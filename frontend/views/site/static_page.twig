{% include 'parts/breadcrumbs.twig' %}
<section class="section-padding">
    <div class="container content-wrapper">
        <div class="row">
            <div class="col-md-12">
                <h1 class="store-h1 static">{{ h1 }}</h1>
                {{ content | raw}}
            </div>
        </div>
    </div>
</section>

{% if dopline %}
    {{ dopline | raw }}
{% endif %}