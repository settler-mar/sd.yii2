<h2>Все завершившиеся акции, купоны и промокоды</h2>
<div  class="catalog-coupons margin">
  {% for coupon in coupon_ended %}
    <div class="accordion">
      <div class="accordion-control accordion-title">{{ coupon.name }}</div>
      <div class="accordion-content">
        {% if  coupon.description%}
          {{ coupon.description }}
        {% else %}
          Дополнительная информация о промокоде отсутствует.
        {% endif %}
      </div>
    </div>
  {% endfor %}
</div>