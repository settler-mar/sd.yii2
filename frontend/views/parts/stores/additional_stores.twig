<section class="margin">
  <div class="page-wrap">
    {% if additional_stores_category %}
      <h2>Популярные магазины из раздела

          {{ additional_stores_category.name }} с&nbsp;кэшбэком

      </h2>
    {% else %}
      <h2>Другие популярные интернет-магазины с кэшбэком</h2>
    {% endif %}
    <div class="shops">
      {% for store in additional_stores %}
        {% include 'parts/shop.twig' %}
      {% endfor %}
      <div class="shop shop-empty"></div>
      <div class="shop shop-empty"></div>
      <div class="shop shop-empty"></div>
      <div class="shop shop-empty"></div>
      <div class="shop shop-empty"></div>
      <div class="shop shop-empty"></div>
    </div>
    <div class="align-center">
      <a href="/stores" class="btn btn-transform">Все магазины с кэшбэком</a>
    </div>
  </div>
</section>