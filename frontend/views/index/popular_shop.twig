<section class="margin">
    <div class="page-wrap">
        <h2 class="content-header">{{ t('main', '{count}_popular_stores', {'count': total_all_stores})|raw }}</h2>
        <div class="shops">
            {% for store in stores %}
                {% include 'parts/shop.twig' %}
            {% endfor %}
            <div class="shop shop-empty"></div>
            <div class="shop shop-empty"></div>
            <div class="shop shop-empty"></div>
            <div class="shop shop-empty"></div>
            <div class="shop shop-empty"></div>
            <div class="shop shop-empty"></div>
        </div>
        <div class="align-center">
            <a href="{{ _href('/stores')|raw }}" class="btn btn-transform">{{ t('main', 'all_stores_with_cashback')|raw }}</a>
        </div>
    </div>
</section>