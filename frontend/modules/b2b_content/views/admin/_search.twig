{{ use('/yii/widgets/ActiveForm') }}
<div class="b2b-content-search">
    {% set form = active_form_begin({
            'action': ['index'],
            'method': 'get',
        })
    %}

    {{ form.field(model, 'id')|raw }}

    {{ form.field(model, 'page')|raw }}

    {{ form.field(model, 'title')|raw }}

    {{ form.field(model, 'description')|raw }}

    {{ form.field(model, 'keywords')|raw }}

    {# form.field(model, 'h1')|raw #}

    {# form.field(model, 'content')|raw #}

    {# form.field(model, 'menu_show')|raw #}

    {# form.field(model, 'menu_index')|raw #}

    <div class="form-group">
        <input type="submit" value="{{ 'Search' }}" class="btn btn-primary">
        <input type="reset" value="{{ 'Reset' }}" class="btn btn-default">
    </div>
    {{ active_form_end() }}
</div>