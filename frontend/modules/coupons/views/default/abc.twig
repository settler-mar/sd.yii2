{% if h1 %}
    {#h1 из базы#}
    {% set h1_final =  h1 %}
{% else %}
    {#если нет, то#}
    {% set h1_final = t('main','coupons_abc_default_h1<a href="{href}">stores</a>_{count}', {'count': total_v , 'href': _href('/stores') }) %}
{% endif%}

    {#поле контент из метатегов#}
    {% if content %}
        <section class="gray-box wrap-round margin">
          <h1 class="align-center laptop-no-margin-bottom">{{ h1_final|raw }}</h1>
            <div class="laptop_hide">
                {{ content | raw}}
            </div>
        </section>
    {% endif %}

    <section class="catalog margin">
        <div class="page-wrap page-wrap-flex">
            {#слева категории#}

            <div class="catalog-categories">
                {% include('left_menu.twig') %}
            </div>
            {#справа контент#}
            <div class="catalog-stores coupons">

                {% if content == null or content == '' %}
                    <h1 >{{ h1_final|raw }}</h1>
                {% endif %}

                <div class="catalog-categories_tree-toggle">
                    {{ svg('bars-slim', 'catalog-categories_tree-toggle-icon catalog-categories_tree-toggle-icon-close')|raw }}
                    {{ svg('close', 'catalog-categories_tree-toggle-icon catalog-categories_tree-toggle-icon-open')|raw }}
                    <span class="catalog-categories_tree-toggle-tittle">{{ t('main', 'stores_change_category') }}</span>
                </div>

                <div class="coupons-abc">
                    <div  class="coupons-abc_chars">
                        {% for char, stores in stores_abc|slice(0, 27) %}
                            {% if stores|length %}
                                <a href="#store{{ char|raw }}" class="scroll_to">{{ char|raw }} </a>
                            {% else %}
                                <span>{{ char }}</span>
                            {% endif %}
                        {% endfor %}
                    </div>
                    <div  class="coupons-abc_chars row-2">
                        {% for char, stores in stores_abc|slice(27) %}
                            {% if stores|length %}
                                <a href="#store{{ char|raw }}"  class="scroll_to">{{ char|raw }}</a>
                            {% else %}
                                <span>{{ char }}</span>
                            {% endif %}
                        {% endfor %}
                    </div>
                </div>

                <div  class="coupons-abc_list">
                    {% for char, stores in stores_abc %}
                        {% if stores|length %}
                            <div id="store{{ char|raw }}" class="coupons-abc_list-item">
                                <div class="coupons-abc_list-item-char">{{ char|raw }}</div>
                                <div class="coupons-abc_list-item-stores">
                                    {% for store in stores %}
                                        <a href="{{ _href('/coupons/'~store.route)|raw }}">{{ store.name}}</a>
                                    {% endfor %}
                                </div>
                            </div>

                        {% endif %}
                    {% endfor %}
                </div>
            </div>
        </div>
    </section>
