{% if categories|length %}
    <ul class="shops-category-menu_list" >
        {% for category in categories  %}
            <li class="shops-category-menu_item {% if category.childs|length %} menu-group{% endif %} {% if category.current %}open{% endif %}">
                {% if category.id == current %}
                    <span class="active">{{ category.name|raw }} {% if category.count_all %}({{ category.count_all }}){% endif %}
                        {% if category.childs|length %}
                            {{ svg('angle-down', 'menu_angle-down', '@frontend')|raw }}
                        {% endif %}
                    </span>
                {% else %}
                    <a class="shops-category-menu_link" href="/category/{{ category.full_route }}">{{ category.name|raw }} {% if category.count_all %}({{ category.count_all }}){% endif %}
                        {% if category.childs|length %}
                            {{ svg('angle-down', 'menu_angle-down', '@frontend')|raw }}
                        {% endif %}
                    </a>
                {% endif %}
                {% if category.childs and category.childs|length %}
                    {% set categories = category.childs %}
                    {% include('category_menu.twig') %}
                {% endif %}
            </li>
        {% endfor %}
    </ul>
{% endif %}